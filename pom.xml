<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>4.1.0</version>
		<relativePath/>
	</parent>
	<groupId>io.github.kszuba1</groupId>
	<artifactId>jooq-hibernate-comparison</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>
	<name>jooq-hibernate-comparison</name>
	<description>Master's thesis: comparative analysis of jOOQ and Hibernate</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag/>
		<url/>
	</scm>

	<modules>
		<module>core</module>
		<module>db</module>
		<module>persistence-hibernate</module>
		<module>persistence-jooq</module>
		<module>benchmarks</module>
	</modules>

	<properties>
		<java.version>21</java.version>
		<jmh.version>1.37</jmh.version>
		<datasource-proxy.version>1.11.0</datasource-proxy.version>
		<maven-enforcer-plugin.version>3.6.3</maven-enforcer-plugin.version>
		<gmavenplus-plugin.version>5.1.0</gmavenplus-plugin.version>
		<groovy.version>4.0.32</groovy.version>
		<postgres.image>postgres:16-alpine</postgres.image>
	</properties>

	<dependencyManagement>
		<dependencies>
			<dependency>
				<groupId>org.openjdk.jmh</groupId>
				<artifactId>jmh-core</artifactId>
				<version>${jmh.version}</version>
			</dependency>
			<dependency>
				<groupId>org.openjdk.jmh</groupId>
				<artifactId>jmh-generator-annprocess</artifactId>
				<version>${jmh.version}</version>
			</dependency>
			<dependency>
				<groupId>net.ttddyy</groupId>
				<artifactId>datasource-proxy</artifactId>
				<version>${datasource-proxy.version}</version>
			</dependency>
		</dependencies>
	</dependencyManagement>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.codehaus.gmavenplus</groupId>
					<artifactId>gmavenplus-plugin</artifactId>
					<version>${gmavenplus-plugin.version}</version>
				</plugin>
				<plugin>
					<groupId>org.flywaydb</groupId>
					<artifactId>flyway-maven-plugin</artifactId>
					<version>${flyway.version}</version>
				</plugin>
				<plugin>
					<groupId>org.jooq</groupId>
					<artifactId>jooq-codegen-maven</artifactId>
					<version>${jooq.version}</version>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-enforcer-plugin</artifactId>
					<version>${maven-enforcer-plugin.version}</version>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-enforcer-plugin</artifactId>
				<executions>
					<execution>
						<id>enforce-thesis-rules</id>
						<goals>
							<goal>enforce</goal>
						</goals>
						<configuration>
							<rules>
								<requireJavaVersion>
									<version>[21,)</version>
								</requireJavaVersion>
								<requireMavenVersion>
									<version>[3.9,)</version>
								</requireMavenVersion>
								<bannedDependencies>
									<excludes>
										<exclude>com.h2database:h2</exclude>
										<exclude>org.hsqldb:hsqldb</exclude>
										<exclude>org.apache.derby:derby</exclude>
										<exclude>org.projectlombok:lombok</exclude>
										<exclude>org.mapstruct:*</exclude>
										<exclude>org.springframework.data:spring-data-jpa</exclude>
									</excludes>
									<searchTransitive>true</searchTransitive>
									<message>Banned dependency found - in-memory databases, boilerplate generators and Spring Data are off-limits; they would invalidate the thesis measurements.</message>
								</bannedDependencies>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>
